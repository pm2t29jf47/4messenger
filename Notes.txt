????
- SqlMembershipProvider

!!!
- добавить namespaces в обработчики искл
- фильтр
- убрать возможность выбора в sidebar-е
- обработка исключений (now) (не ловить в прокси)

####
i)ValidationRule вызывается только при пользовательском вводе,
при программном изменении binding свойства объектной модели ValidationRule не вызывается

ii) Никаких дополнительных преобразований в аксессорах binding свойств 
(если новое значение после самодельных преобразований(!) в set аксессоре равно старому, 
то новое значение не обновится в GUI несмотря на явный вызов PropertyChanged)